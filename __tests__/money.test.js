// import {Object } from '../src/money.js';

describe('money', () => {
	var money;

	beforeEach(() => {
		money = new Triangle(5, 5, 5);
	});

	test('your test', () => {
		expect('this').toEqual('that');
	});
});
